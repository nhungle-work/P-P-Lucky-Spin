export interface LeadData {
  name: string;
  phone: string;
  email: string;
  company: string;
}

export interface Prize {
  id: string;
  name: string;
  icon: string;
}

export interface Inventory {
  tag: number;
  notebook: number;
}
